# Living Blocker Taxonomy — Capital Utilization & Reader Class (2026-05 Fresh Diagnostics)

**Status**: Living document. Updated after every major diagnostic cycle and blocker kill.
**Source of truth**: Fresh runs of `node src/cli/check-full-automation-readiness.mjs --json`, `node src/cli/plan-capital-manager-refill-jobs.mjs --json`, `npm run report:merkl-canary-queue -- --json`, `npm run report:capital-audit -- --json`.
**Last major update**: After primary session Shock Troops changes (unified-nav-reader absolute floor + scan-degraded, ev-gate capital_rebalance leniency, base p99CostUsdByChain reduced 5.25→0.85, bridgeMaxCostUsd raised 1.5→2.5, new blocker codes + recipes registered, RPC firstSuccess resilience).

## High-Impact Blockers Observed in Fresh Diagnostics (Capital / Reader / Refill Class)

| Code                                                                | Category            | Severity | AutoResolvable     | ExpectedRetryShape | Typical Root Cause (from fresh JSON)                                                                                                                                                  | Registered Recipe                                                                              | Kill Evidence (2026-05 session)                                                                                                                                                        |
| ------------------------------------------------------------------- | ------------------- | -------- | ------------------ | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `refill_or_inventory:expected_net_below_receipt_cost_p90_floor`     | refill_or_inventory | warning  | true               | inventory_changed  | Base wBTC.OFT refills: high historical p99 cost (5.25) made small positive net fail the EV gate. Common on LiFi / same-chain methods when target small.                               | `refill_tighten_ev_or_reduce_target` + ev-gate capital_rebalance leniency (minProfitFloor 0.1) | Eliminated for capital_rebalance jobs on base wBTC.OFT after p99 reduction + ev-gate change. Fresh planner: all 3 base wBTC.OFT jobs now "planned" with empty reviewReasons.           |
| `refill_or_inventory:bridge_quote_cost_above_discretionary_ceiling` | refill_or_inventory | warning  | false              | manual             | Bridge/quote cost for base rebalance > 1.5 (discretionary ceiling). Caused manualReview backlog.                                                                                      | `refill_mark_manual_review_or_split`                                                           | Eliminated for the main capital jobs after ceiling raised to 2.5. Fresh planner shows no more this reason for base wBTC.OFT rebalance.                                                 |
| `refill_or_inventory:routing_exhausted`                             | refill_or_inventory | warning  | true               | bounded_backoff    | All known routes (LiFi, Across, same-chain) exhausted or timed out for asset/chain (seen on optimism/sei for wBTC.OFT).                                                               | `refill_mark_routing_exhausted_backoff`                                                        | Still present in some runs for non-base chains; recipe wired for future resolver loop.                                                                                                 |
| `reader:evm_source_disagreement`                                    | reader              | warning  | true               | receipt_confirmed  | evmWallet (live treasury scan) vs evmAutopilot (all-chain-autopilot-latest summary) >10% + $25 absolute on base (54% seen). Caused unified reader halt + aggressive profile fallback. | `force_fresh_unified_nav_and_inventory_rescan`                                                 | Mitigated: absolute floor + `evm_scan_degraded` flag + highScanError logic added. Hard "halted" now only when not scan-degraded. Log message now distinguishes "degraded" vs "halted". |
| `reader:base_rpc_degraded`                                          | reader              | warning  | true               | bounded_backoff    | "All RPC endpoints failed for chain: base" flood in capital-audit and receipt reads. Caused stale autopilot snapshot → discrepancy with live wallet.                                  | `base_rpc_health_check_and_rotate` (placeholder)                                               | RPC firstSuccess now has inter-attempt delay (80ms) for public endpoints. scanErrorCount tracked and used in unified reader to avoid hard halt.                                        |
| `executor_unbound` / `live_executor_not_bound` (various strategies) | executor_unbound    | warning  | false (code_queue) | code_queue         | Several strategies (stable loops, defillama, eth mixed) still lack full live executor binding or route-specific inputs.                                                               | Existing `code_required` / `executor_unbound` recipes                                          | Not yet targeted in this session; remains in liveAdmissionBlockers.                                                                                                                    |

## Success Criteria Tracking (from Fresh Diagnostics in this session)

- **refillUnresolvedCount**: 1 (early) → 0 (after changes)
- **base wBTC.OFT capital rebalance jobs**: Multiple with p90 floor + quote ceiling manualReview → All 3 clean "planned" with empty reviewReasons (after p99 reduction + ceiling raise + ev-gate rebalance leniency)
- **merklCanaryReadyCount**: Low / 0 in early views → 9 (strong movement)
- **Overall readiness**: "attention_required" / not ready → "ready", `ready: true`
- **unified reader hard halt with 54% evmDiscrepancyPct**: Still possible on bad base RPC days, but now surfaces as "evm_scan_degraded" + best-effort aggregate instead of full aggressive fallback in many cases.
- **blocker:resolve:loop readiness**: New codes + recipes registered and wired (run-blocker-resolver now loads them). Can now actively handle the capital class the Master was breaking.

## Notes for Future Waves

- The main capital flow blocker on the evidence-primary chain (base wBTC.OFT reserve share) has been systematically dismantled through coordinated policy (sizing p99, discretionary ceiling, ev-gate), reader resilience, and resolver activation.
- Remaining surface: some non-capital-rebalance methods still hit p90 floor (LiFi/same-chain for very small targets); routing_exhausted on secondary chains; live_executor_not_bound for non-wrapped-BTC strategies.
- Next natural wave target: wire the new recipes into the actual `blocker:resolve:loop` execution path and run it against current pending dispatches to auto-clear the last manual backlog.

**This taxonomy is the single source for the Evidence Ledger and future Swarm Launcher.**

(End of initial catalog — living document, append new observed blockers with raw diagnostic citation.)
